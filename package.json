{
  "name": "techshare-decorators",
  "version": "1.0.0",
  "description": "Example decorators",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p dist/assets && cp src/index.html dist/index.html && cp src/assets/*.* dist/assets/ && cp src/*.css dist/ && babel src -d lib && webpack --mode=development lib/js/index.js -o dist/bundle.js",
    "serve": "serve dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-decorators-legacy": "^1.3.5",
    "babel-preset-env": "^1.7.0",
    "babel-watch": "^2.0.7",
    "serve": "^9.1.1",
    "webpack": "^4.14.0",
    "webpack-cli": "^3.0.8"
  }
}
